const eval = require('./repl');

const s1 = 'dependency or good first issue'
const s2 = '(good first issue and dependency) or (good second issue and backend'
const s3 = 'not (good first issue and dependency)'
const s4 = 'not good first issue'

console.log(eval(1, 2))